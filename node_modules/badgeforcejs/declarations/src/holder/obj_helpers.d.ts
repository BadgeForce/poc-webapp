export declare class HolderInfo {
    address: string;
    /**
     * Holder info object helper
     * @param address address of the holder
     */
    constructor(address: string);
}
export declare class Credential {
    issuer: string;
    description: string;
    name: string;
    image: string;
    version: string;
    expires: string;
    recipient: string;
    txKey: string;
    active: boolean;
    /**
     * Credential object helper
     * @param issuer      address of the issuer who issued this credential
     * @param description
     * @param name
     * @param image
     * @param version
     * @param expires     expiration date
     * @param recipient   address of the holder contract
     * @param txKey       transaction key of the credential
     * @param active      flag for pending credentials
     */
    constructor(issuer?: string, description?: string, name?: string, image?: string, version?: string, expires?: string, recipient?: string, txKey?: string, active?: boolean);
}
