const IssuerFactory = artifacts.require(`./IssuerFactory.sol`);
const BadgeLibrary = artifacts.require("BadgeLibrary.sol");

module.exports = (deployer) => { 
  deployer.deploy([BadgeLibrary]);
  deployer.link(BadgeLibrary, [IssuerFactory]); 
  deployer.deploy(IssuerFactory, {gas: 5712389});
}