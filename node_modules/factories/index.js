const issuer_factory_artifacts = require("./build/contracts/IssuerFactory.json");
const holder_factory_artifacts = require("./build/contracts/HolderFactory.json");

module.exports = {
    issuerFactory: issuer_factory_artifacts, 
    holderFactory: holder_factory_artifacts
}