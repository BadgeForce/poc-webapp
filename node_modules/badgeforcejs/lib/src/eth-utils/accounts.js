"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = y[op[0] & 2 ? "return" : op[0] ? "throw" : "next"]) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [0, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
var Wallet = require("ethereumjs-wallet");
var importers = require("ethereumjs-wallet/thirdparty");
var ethUtil = require("ethereumjs-util");
var obj_helpers = require("./obj_helpers");
/**
 * @param wallet  wallet object created with 'ethereumjs-wallet'
 * @return private key hex
 */
exports.walletToPrivateKey = function (wallet) {
    var privateKeyBuffer = wallet.getPrivateKey();
    return ethUtil.bufferToHex(privateKeyBuffer);
};
var AccountImporter = /** @class */ (function () {
    function AccountImporter() {
    }
    /**
     * @param privateKey  private key hex for an account
     * @return promise that resolves to an [[Account]] object
     */
    AccountImporter.prototype.importAccountRaw = function (privateKey) {
        return __awaiter(this, void 0, void 0, function () {
            var privateKeyHex, address;
            return __generator(this, function (_a) {
                try {
                    privateKeyHex = ethUtil.stripHexPrefix(privateKey);
                    address = ethUtil.privateToAddress(ethUtil.toBuffer(ethUtil.addHexPrefix(privateKeyHex)));
                    return [2 /*return*/, new obj_helpers.Account(privateKeyHex, ethUtil.bufferToHex(address))];
                }
                catch (e) {
                    console.log(e);
                    throw new Error('Unable to import account');
                }
                return [2 /*return*/];
            });
        });
    };
    /**
     * @param input  EtherWallet JSON
     * @param password  password for the account
     * @return promise that resolves to an [[Account]] object
     */
    AccountImporter.prototype.importAccountJSON = function (input, password) {
        return __awaiter(this, void 0, void 0, function () {
            var wallet, privateKeyHex, address;
            return __generator(this, function (_a) {
                try {
                    wallet = void 0;
                    try {
                        wallet = importers.fromEtherWallet(input, password);
                    }
                    catch (e) {
                        console.log('Attempt to import as EtherWallet format failed, trying V3...');
                    }
                    if (!wallet) {
                        wallet = Wallet.fromV3(input, password, true);
                    }
                    privateKeyHex = exports.walletToPrivateKey(wallet);
                    address = ethUtil.privateToAddress(privateKeyHex);
                    return [2 /*return*/, new obj_helpers.Account(privateKeyHex, ethUtil.bufferToHex(address))];
                }
                catch (e) {
                    console.log(e);
                    throw new Error('Unable to import account');
                }
                return [2 /*return*/];
            });
        });
    };
    return AccountImporter;
}());
exports.AccountImporter = AccountImporter;
//# sourceMappingURL=accounts.js.map